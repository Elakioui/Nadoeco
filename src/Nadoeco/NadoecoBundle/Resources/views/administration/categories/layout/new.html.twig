{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Categories creation</h1>
    <form action="{{ path('categories_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form(form) }}
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('categoriesAdmin') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
